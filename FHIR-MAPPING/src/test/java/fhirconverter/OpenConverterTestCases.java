package fhirconverter;
import static org.junit.Assert.*;
import org.junit.*; 
import org.json.JSONException; 
import org.json.JSONObject;

public class OpenConverterTestCases {
/*	
	@Test

	public void testPatientIntializeWorkFlow(){
		ConverterOpenempi tester = new ConverterOpenempi();
		String dataRead = tester.patientRead("10");	
		JSONObject jsonObj = new JSONObject(dataRead); //READ
		Assert.assertTrue(jsonObj.has("resourceType"));
		String dataUpdate = tester.patientUpdate("10"); //UPDATE
		JSONObject jsonUpdateObj = new JSONObject(dataUpdate);
		Assert.assertTrue(jsonUpdateObj.has("resourceType"));
	}
*/
}

